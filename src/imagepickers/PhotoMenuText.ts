import styled from 'styled-components/native';
import CustomText from './CustomText';

const PhotoMenuText = styled(CustomText)`
  font-family: Exo2-SemiBold;
  color: #00040d;
  font-size: 22px;
`;

export default PhotoMenuText;
